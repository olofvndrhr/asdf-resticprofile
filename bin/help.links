#!/usr/bin/env bash

set -euo pipefail

current_script_path=${BASH_SOURCE[0]}
plugin_dir=$(dirname "$(dirname "${current_script_path}")")

# shellcheck source=../lib/utils.bash
source "${plugin_dir}/lib/utils.bash"

cat << EOF
Links:
- asdf plugin repository: https://github.com/olofvndrhr/asdf-resticprofile
- tool repository: https://github.com/creativeprojects/resticprofile
- tool website: https://creativeprojects.github.io/resticprofile/

EOF
